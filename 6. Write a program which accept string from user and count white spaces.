#include<stdio.h>
int WhiteSpace(char *str)
{
	int icnt=0;
	while(*str!='\0')
	{
		if(*str==' ')
		{
			icnt++;
		}
		str++;
	}
	return icnt;
}

int main()
{
	char Arr[20];
	int iRet=0;
  
	printf("Enter string");
	scanf("%[^'\n']s",Arr);

	iRet=WhiteSpace(Arr);
	printf("%d",iRet);
  
	return 0;
}
